import { Client, PermissionString } from "discord.js";
export interface IOptionData {
    emoji: string;
    addMessage: string;
    removeMessage: string;
    roles: string[];
}
export interface IMessageData {
    messageID: string;
    channelID: string;
    limit: number;
    restrictions?: PermissionString[];
    reactions: IOptionData[];
}
export interface IConfig {
    [messageID: string]: IMessageData;
}
export declare class ReactionRole extends Client {
    private _token;
    private database;
    private ready;
    constructor(token: string, mongodb_uri?: string);
    createOption(emoji: string, addMessage: string, removeMessage: string, roles: string[]): IOptionData;
    createMessage(messageID: string, channelID: string, limit: number, restrictions: PermissionString[], ...reactions: IOptionData[]): Promise<IMessageData>;
    deleteMessage(messageID: string): Promise<IConfig>;
    importConfig(config: IConfig): Promise<IConfig>;
    exportConfig(): Promise<IConfig>;
    init(): Promise<string>;
    reInit(): Promise<ReactionRole>;
}
